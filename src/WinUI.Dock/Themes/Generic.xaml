<!--
    This code was generated by a tool.
    Changes to this file may cause incorrect behavior and will be lost if the code is regenerated.
-->
<!--
Source files:
DockManager.xaml
Document.xaml
DocumentGroup.xaml
LayoutPanel.xaml
-->
<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:dock="using:WinUI.Dock" xmlns:toolkit="using:CommunityToolkit.WinUI.Controls">
  <Style TargetType="dock:DockManager">
    <Setter Property="AllowDrop" Value="True" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="dock:DockManager">
          <Grid>
            <Grid.RowDefinitions>
              <RowDefinition Height="Auto" />
              <RowDefinition Height="*" />
              <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="Auto" />
              <ColumnDefinition Width="*" />
              <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <VisualStateManager.VisualStateGroups>
              <VisualStateGroup x:Name="OperationDockTargets">
                <VisualState x:Name="ShowSideDockTargets">
                  <VisualState.Setters>
                    <Setter Target="DockTargets.Visibility" Value="Visible" />
                    <Setter Target="DockTargetCenter.Visibility" Value="Collapsed" />
                  </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="ShowAllDockTargets">
                  <VisualState.Setters>
                    <Setter Target="DockTargets.Visibility" Value="Visible" />
                    <Setter Target="DockTargetCenter.Visibility" Value="Visible" />
                  </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="HideDockTargets">
                  <VisualState.Setters>
                    <Setter Target="DockTargets.Visibility" Value="Collapsed" />
                  </VisualState.Setters>
                </VisualState>
              </VisualStateGroup>
            </VisualStateManager.VisualStateGroups>
            <!--
                            If you use GridSplitter directly in the cs file,
                            the style of GridSplitter will have issues.
                            So, create a hidden GridSplitter in the xaml file first,
                            to ensure the style of GridSplitter is displayed correctly.
                        -->
            <toolkit:GridSplitter Visibility="Collapsed" />
            <dock:Sidebar Grid.Row="1" Grid.Column="0" Documents="{Binding LeftSide, RelativeSource={RelativeSource TemplatedParent}}" Manager="{Binding RelativeSource={RelativeSource TemplatedParent}}" Side="Left" />
            <dock:Sidebar Grid.Row="0" Grid.Column="1" Documents="{Binding TopSide, RelativeSource={RelativeSource TemplatedParent}}" Manager="{Binding RelativeSource={RelativeSource TemplatedParent}}" Side="Top" />
            <dock:Sidebar Grid.Row="1" Grid.Column="2" Documents="{Binding RightSide, RelativeSource={RelativeSource TemplatedParent}}" Manager="{Binding RelativeSource={RelativeSource TemplatedParent}}" Side="Right" />
            <dock:Sidebar Grid.Row="2" Grid.Column="1" Documents="{Binding BottomSide, RelativeSource={RelativeSource TemplatedParent}}" Manager="{Binding RelativeSource={RelativeSource TemplatedParent}}" Side="Bottom" />
            <ContentPresenter Grid.Row="1" Grid.Column="1" Content="{TemplateBinding Panel}" />
            <Border x:Name="PART_PopupContainer" Grid.Row="1" Grid.Column="1" />
            <Grid x:Name="DockTargets" Grid.Row="1" Grid.Column="1" Visibility="Collapsed">
              <dock:DockTargetButton x:Name="DockTargetCenter" HorizontalAlignment="Center" VerticalAlignment="Center" Destination="{Binding RelativeSource={RelativeSource TemplatedParent}}" Target="Center" />
              <dock:DockTargetButton HorizontalAlignment="Left" VerticalAlignment="Center" Destination="{Binding RelativeSource={RelativeSource TemplatedParent}}" Target="DockLeft" />
              <dock:DockTargetButton HorizontalAlignment="Center" VerticalAlignment="Top" Destination="{Binding RelativeSource={RelativeSource TemplatedParent}}" Target="DockTop" />
              <dock:DockTargetButton HorizontalAlignment="Right" VerticalAlignment="Center" Destination="{Binding RelativeSource={RelativeSource TemplatedParent}}" Target="DockRight" />
              <dock:DockTargetButton HorizontalAlignment="Center" VerticalAlignment="Bottom" Destination="{Binding RelativeSource={RelativeSource TemplatedParent}}" Target="DockBottom" />
            </Grid>
            <dock:Preview x:Name="PART_Preview" Grid.Row="1" Grid.Column="1" Visibility="Collapsed" />
          </Grid>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
  <Style TargetType="dock:Document">
    <Setter Property="Padding" Value="16" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="dock:Document">
          <Border Background="Transparent">
            <ContentPresenter Padding="{TemplateBinding Padding}" Content="{TemplateBinding Content}" />
          </Border>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
  <Style TargetType="dock:LayoutPanel">
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="dock:LayoutPanel">
          <Grid x:Name="PART_Root" ColumnSpacing="12" RowSpacing="12">
            <Grid.Resources>
              <Style x:Key="PART_DefaultSplitterStyle" TargetType="toolkit:GridSplitter">
                <Setter Property="IsTabStop" Value="True" />
                <Setter Property="UseSystemFocusVisuals" Value="True" />
                <Setter Property="IsFocusEngagementEnabled" Value="True" />
                <Setter Property="MinHeight" Value="12" />
                <Setter Property="MinWidth" Value="12" />
                <Setter Property="Padding" Value="4" />
                <Setter Property="Foreground" Value="{ThemeResource ControlStrongFillColorDefaultBrush}" />
                <Setter Property="Background" Value="{ThemeResource ControlAltFillColorTransparentBrush}" />
                <Setter Property="ManipulationMode" Value="TranslateX,TranslateY" />
                <Setter Property="Template">
                  <Setter.Value>
                    <ControlTemplate TargetType="toolkit:GridSplitter">
                      <Grid Background="Transparent">
                        <VisualStateManager.VisualStateGroups>
                          <VisualStateGroup x:Name="CommonStates">
                            <VisualState x:Name="Normal" />
                            <VisualState x:Name="PointerOver">
                              <VisualState.Setters>
                                <Setter Target="VerticalTrack.Background" Value="{ThemeResource ControlAltFillColorTertiaryBrush}" />
                                <Setter Target="HorizontalTrack.Background" Value="{ThemeResource ControlAltFillColorTertiaryBrush}" />
                              </VisualState.Setters>
                            </VisualState>
                            <VisualState x:Name="Pressed">
                              <VisualState.Setters>
                                <Setter Target="VerticalTrack.Background" Value="{ThemeResource ControlAltFillColorQuarternaryBrush}" />
                                <Setter Target="HorizontalTrack.Background" Value="{ThemeResource ControlAltFillColorQuarternaryBrush}" />
                              </VisualState.Setters>
                            </VisualState>
                            <VisualState x:Name="Disabled">
                              <VisualState.Setters>
                                <Setter Target="VerticalTrack.Background" Value="{ThemeResource ControlAltFillColorDisabledBrush}" />
                                <Setter Target="HorizontalTrack.Background" Value="{ThemeResource ControlAltFillColorDisabledBrush}" />
                                <Setter Target="PART_Thumb.Opacity" Value="0.45" />
                                <Setter Target="HorizontalThumb.Opacity" Value="0.45" />
                              </VisualState.Setters>
                            </VisualState>
                          </VisualStateGroup>
                          <VisualStateGroup x:Name="OrientationStates">
                            <VisualState x:Name="Vertical" />
                            <VisualState x:Name="Horizontal">
                              <VisualState.Setters>
                                <Setter Target="VerticalTrack.Visibility" Value="Collapsed" />
                                <Setter Target="HorizontalTrack.Visibility" Value="Visible" />
                              </VisualState.Setters>
                            </VisualState>
                          </VisualStateGroup>
                          <VisualStateGroup x:Name="ThumbVisibilityStates">
                            <VisualState x:Name="Visible" />
                            <VisualState x:Name="Collapsed">
                              <VisualState.Setters>
                                <Setter Target="PART_Thumb.Visibility" Value="Collapsed" />
                                <Setter Target="HorizontalThumb.Visibility" Value="Collapsed" />
                              </VisualState.Setters>
                            </VisualState>
                          </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>
                        <Grid x:Name="VerticalTrack" Width="{Binding Path=(dock:LayoutPanel.SplitterTrackThickness), RelativeSource={RelativeSource TemplatedParent}}" HorizontalAlignment="Center" VerticalAlignment="Stretch" Background="{TemplateBinding Background}">
                          <Grid.BackgroundTransition>
                            <BrushTransition Duration="0:0:0.083" />
                          </Grid.BackgroundTransition>
                          <Rectangle x:Name="PART_Thumb" Width="4" Height="24" Fill="{TemplateBinding Foreground}" RadiusX="2" RadiusY="2" />
                        </Grid>
                        <Grid x:Name="HorizontalTrack" Height="{Binding Path=(dock:LayoutPanel.SplitterTrackThickness), RelativeSource={RelativeSource TemplatedParent}}" HorizontalAlignment="Stretch" VerticalAlignment="Center" Background="{TemplateBinding Background}" Visibility="Collapsed">
                          <Grid.BackgroundTransition>
                            <BrushTransition Duration="0:0:0.083" />
                          </Grid.BackgroundTransition>
                          <Rectangle x:Name="HorizontalThumb" Width="24" Height="4" Fill="{TemplateBinding Foreground}" RadiusX="2" RadiusY="2" />
                        </Grid>
                      </Grid>
                    </ControlTemplate>
                  </Setter.Value>
                </Setter>
              </Style>
            </Grid.Resources>
          </Grid>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
  <Style TargetType="dock:DocumentGroup">
    <Setter Property="AllowDrop" Value="True" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="dock:DocumentGroup">
          <Grid>
            <VisualStateManager.VisualStateGroups>
              <VisualStateGroup x:Name="IsActive">
                <VisualState x:Name="Active">
                  <VisualState.Setters>
                    <Setter Target="PART_Root.BorderBrush" Value="{ThemeResource DockStrokeActiveBrush}" />
                  </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="Inactive" />
              </VisualStateGroup>
              <VisualStateGroup x:Name="TabPosition">
                <VisualState x:Name="Top">
                  <VisualState.Setters>
                    <Setter Target="Scale.ScaleY" Value="1" />
                  </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="Bottom">
                  <VisualState.Setters>
                    <Setter Target="Scale.ScaleY" Value="-1" />
                  </VisualState.Setters>
                </VisualState>
              </VisualStateGroup>
              <VisualStateGroup x:Name="OperationDockTargets">
                <VisualState x:Name="ShowDockTargets">
                  <VisualState.Setters>
                    <Setter Target="DockTargets.Visibility" Value="Visible" />
                  </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="HideDockTargets">
                  <VisualState.Setters>
                    <Setter Target="DockTargets.Visibility" Value="Collapsed" />
                  </VisualState.Setters>
                </VisualState>
              </VisualStateGroup>
            </VisualStateManager.VisualStateGroups>
            <TabView x:Name="PART_Root" VerticalAlignment="Stretch" RenderTransformOrigin="0.5,0.5" SelectedIndex="{Binding SelectedIndex, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}" Style="{StaticResource TabViewStyle}" TabWidthMode="Compact">
              <TabView.RenderTransform>
                <ScaleTransform x:Name="Scale" />
              </TabView.RenderTransform>
            </TabView>
            <Grid x:Name="DockTargets" Width="124" Height="124" HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="Collapsed">
              <Path Data="m44,0 l36,0 a4,4,0,0,1,4,4 l0,32 a4,4,0,0,0,4,4 l32,0 a4,4,0,0,1,4,4 l0,36 a4,4,0,0,1,-4,4 l-32,0 a4,4,0,0,0,-4,4 l0,32 a4,4,0,0,1,-4,4 l-36,0 a4,4,0,0,1,-4,-4 l0,-32 a4,4,0,0,0,-4,-4 l-32,0 a4,4,0,0,1,-4,-4 l0,-36 a4,4,0,0,1,4,-4 l32,0 a4,4,0,0,0,4,-4 l0,-32 a4,4,0,0,1,4,-4 z" Fill="{ThemeResource CardBackgroundFillColorDefaultBrush}" Stretch="Uniform" Stroke="{ThemeResource CardStrokeColorDefaultBrush}" StrokeThickness="1" />
              <Grid Width="120" Height="120" Margin="2">
                <Grid.RowDefinitions>
                  <RowDefinition Height="*" />
                  <RowDefinition Height="*" />
                  <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                  <ColumnDefinition Width="*" />
                  <ColumnDefinition Width="*" />
                  <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <dock:DockTargetButton Grid.Row="1" Grid.Column="1" Margin="2" Destination="{Binding RelativeSource={RelativeSource TemplatedParent}}" Target="Center" />
                <dock:DockTargetButton Grid.Row="1" Grid.Column="0" Margin="2" Destination="{Binding RelativeSource={RelativeSource TemplatedParent}}" Target="SplitLeft" />
                <dock:DockTargetButton Grid.Row="0" Grid.Column="1" Margin="2" Destination="{Binding RelativeSource={RelativeSource TemplatedParent}}" Target="SplitTop" />
                <dock:DockTargetButton Grid.Row="1" Grid.Column="2" Margin="2" Destination="{Binding RelativeSource={RelativeSource TemplatedParent}}" Target="SplitRight" />
                <dock:DockTargetButton Grid.Row="2" Grid.Column="1" Margin="2" Destination="{Binding RelativeSource={RelativeSource TemplatedParent}}" Target="SplitBottom" />
              </Grid>
            </Grid>
            <dock:Preview x:Name="PART_Preview" Visibility="Collapsed" />
          </Grid>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
</ResourceDictionary>